# Day 1 notes:

1. `input()` always returns string

2. Type conversion creates a new object, it doesnt modify the real one

3. strings are immutable. Their transformation requires to allocate new memory elsewhere.

